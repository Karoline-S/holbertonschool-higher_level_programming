#!/usr/bin/python3
#!/usr/bin/python3
import random
number = random.randint(-10000, 10000)

if number < 0:
    last_digit = int(repr(number)[0])
else
    print("Last digit of", number, "is", last_digit, "and is greater than 
